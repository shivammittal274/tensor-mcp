# tensor-mcp

tensor-mcp is a local-first MCP gateway CLI. Connect your SaaS accounts (Linear, Slack, Gmail, etc.) via OAuth, tokens stay in your OS keychain, and any MCP client (Claude Desktop, Cursor) can use them. Built on Bun + TypeScript.
