node_modules/
.env
.env.local
dist/
*.sqlite
*.sqlite-journal
vendored/*/venv/
vendored/*/node_modules/
.DS_Store
*.log

# Python bytecode (vendored services run as subprocesses; bytecode is per-machine)
__pycache__/
*.pyc
*.pyo

.embedding-cache/
packages/core/src/embeddings/libs/active/

# Local backup of obsolete files (publish-embeddings.ts + source
# libs/model that used to feed the embeddings-v1 GitHub release). The
# binary no longer reads from these paths — see install.sh which now
# pulls from HuggingFace + unpkg directly.
.backup/
